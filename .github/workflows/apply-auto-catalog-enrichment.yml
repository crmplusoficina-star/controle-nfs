name: Refinar PROCX automático do inventário

on:
  push:
    branches:
      - feat/procx-automatico-inventario
    paths:
      - scripts/refine-auto-catalog-enrichment.py
      - .github/workflows/apply-auto-catalog-enrichment.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/procx-automatico-inventario
      - name: Aplicar refinamento
        run: python scripts/refine-auto-catalog-enrichment.py
      - name: Validar diff
        run: git diff --check && git diff -- app/dashboard/inventory/adjustments/page.tsx
      - name: Commitar página refinada
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/dashboard/inventory/adjustments/page.tsx
          git commit -m "fix: preservar edição manual no preenchimento automático"
          git push
